# Square Race Arena

ブラウザで遊べる観戦型の `Square Race` 実装です。複数の四角が壁と加速帯のあるコースを自動で進み、ぶつかり合いながらゴールを目指します。

## Local

静的サイトなので、任意のローカルサーバーで `index.html` を配信すれば動きます。

例:

```bash
python3 -m http.server 4173
```

## Deploy

このディレクトリをそのまま Vercel にデプロイできます。
